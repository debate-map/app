import {NodeTag} from "./@NodeTag.js";

/*export const tagNodeSlotNames = ["x", "y"];
export function GetNodeTagNodeSlots(tag: NodeTag) {
	const result = tag.type.toLowerCase().split(" ").filter(word=>tagNodeSlotNames.Contains(word));
	if (result.length == 0) {
		return ["x"];
	}
	return result;
}*/