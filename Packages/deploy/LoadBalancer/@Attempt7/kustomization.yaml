resources:

# gateway templates
- gateway-crds
# not needed in kubernetes 1.25+ (according to: https://docs.nginx.com/nginx-gateway-fabric/installation/installing-ngf/helm/)
#- gateway-webhooks/admission_webhook.yaml
#- gateway-webhooks/certificate_config.yaml

# gateway customizations
- gateways.yaml
- routes.yaml

# traefik templates
- rbac.yaml
# traefik customizations: 
# [customizations are set in the traefik-values.yaml file]