resources:

# gateway templates
- gateway-crds

# not needed in kubernetes 1.25+ (according to: https://docs.nginx.com/nginx-gateway-fabric/installation/installing-ngf/helm/)
# - gateway-webhooks-old/admission_webhook.yaml
# - gateway-webhooks-old/certificate_config.yaml

# gateway customizations
- gateways.yaml
- routes.yaml

# templates (maybe not needed anymore; was needed before nginx)
#- rbac.yaml