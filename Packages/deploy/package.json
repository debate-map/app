{
	"name": "dm_deploy",
	"version": "0.0.1",
	"type": "module",
	"private": true,
	"main": "./Pulumi/index.ts",
	"scripts": {
		"start": "cd ../.. && npx nps",
		"#preinstall[disabled; can error in docker]": "npx only-allow yarn"
	},
	"devDependencies": {
		"@pulumi/docker": "^3.0.0",
		"@pulumi/gcp": "^5.0.0",
		"@pulumi/pulumi": "^3.0.0",
		"@types/node": "^20.0.0"
	}
}
